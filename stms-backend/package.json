{
  "name": "stms-backend",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "start": "NODE_ENV=production bun src/index.ts",
    "build:frontend": "cd ../stms-frontend && bun install && bun run build",
    "build": "bun run build:frontend && bun run db:generate",
    "db:migrate": "bun prisma migrate dev",
    "db:seed": "bun prisma/seed.ts",
    "db:generate": "bun prisma generate",
    "db:migrate:prod": "bun prisma migrate deploy",
    "test": "bun test src/tests/api.test.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "^1.3.14"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.4.2",
    "@elysiajs/jwt": "^1.4.2",
    "@prisma/client": "6",
    "@whiskeysockets/baileys": "^7.0.0-rc13",
    "bcryptjs": "^3.0.3",
    "elysia": "^1.4.29",
    "pdf-lib": "^1.17.1",
    "prisma": "6",
    "puppeteer": "^25.3.0",
    "qrcode": "^1.5.4",
    "qrcode-terminal": "^0.12.0"
  }
}
